export default function Hero() {
  return (
    <section className="hero">
      <p className="kicker">Risk strategy lab</p>
      <h1>Make the cost of every control <em>visible.</em></h1>
      <p className="hero-lede">
        Adjust the auto-review threshold and see the projected impact on
        legitimate approvals, fraud loss prevented, and review volume before
        any change reaches production.
      </p>
    </section>
  );
}
